# ==============================================================================
# Environment Configuration Template
# ==============================================================================
# Copy this file to .env and fill in the values
# cp .env.example .env
# ==============================================================================

# ------------------------------------------------------------------------------
# Application Environment
# ------------------------------------------------------------------------------
# Valid values: development | production | test
NODE_ENV=development

# ------------------------------------------------------------------------------
# Server Configuration
# ------------------------------------------------------------------------------
PORT=3000

# ------------------------------------------------------------------------------
# Logging Configuration
# ------------------------------------------------------------------------------
# Log levels: error, warn, info, http, verbose, debug, silly
LOG_LEVEL=info

# Log file paths (optional, recommended for production)
# LOG_FILE_PATH=./logs/app.log
# ERROR_LOG_FILE_PATH=./logs/error.log

# ------------------------------------------------------------------------------
# Database Configuration
# ------------------------------------------------------------------------------
# MongoDB connection URI template with <PASSWORD> placeholder
# The <PASSWORD> placeholder will be automatically replaced with MONGO_PASSWORD
# 
# Format examples:
# - MongoDB Atlas: mongodb+srv://username:<PASSWORD>@cluster0.mongodb.net/database?retryWrites=true&w=majority
# - Local MongoDB: mongodb://username:<PASSWORD>@127.0.0.1:27017/database
# - Docker MongoDB: mongodb://username:<PASSWORD>@mongodb:27017/database
#
# Note: DO NOT URI-encode the password here - it will be automatically encoded
MONGODB_URI=mongodb+srv://username:<PASSWORD>@cluster0.mongodb.net/myapp?retryWrites=true&w=majority

# MongoDB password (will be automatically URI-encoded)
# Can contain special characters like: @ # $ % & etc.
# These will be properly encoded to prevent connection issues
MONGO_PASSWORD=your-mongodb-password

# ------------------------------------------------------------------------------
# Authentication Configuration
# ------------------------------------------------------------------------------
# Application base URL (used for OAuth redirects and email links)
# Development: http://localhost:3000
# Production: https://yourdomain.com
BASE_URL=http://localhost:3000

# Authentication secret for encryption and hashing
# MUST be at least 32 characters with high entropy
# Generate with: openssl rand -base64 32
# CRITICAL: Use different secrets for dev/staging/production
AUTH_SECRET_USER=your-super-secret-key-min-32-chars-change-in-production

# Trusted origins for CORS (optional, comma-separated)
# Example: http://localhost:3000,https://app.yourdomain.com
# Leave empty to allow same-origin only
# TRUSTED_ORIGINS=http://localhost:3000,http://localhost:5173